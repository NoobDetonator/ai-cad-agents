"""Domain modules composed into :class:`aicad.adapters.freecad_adapter.FreeCadAdapter`.

Each module owns one domain of the FreeCAD boundary (context reads, edits,
sketches, features, mechanical parts, documents, export). The public import
path remains ``aicad.adapters.freecad_adapter``.
"""
